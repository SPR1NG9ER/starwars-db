import "./errorMessage.css"

const ErrorMessage = () => {
    return(
        <div className="error-message">
            <div className="error-message-title">Упс что-то пошло не так...</div>
        </div>
    )
}

export default ErrorMessage;